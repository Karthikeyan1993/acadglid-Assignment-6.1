var radius;
radius=5;

var areaOfCircle=Calc(radius);
//Calc function will return area of circle
function Calc(radius) {
  // body...
return ((radius*radius)*Math.PI);
}

console.log(areaOfCircle);
